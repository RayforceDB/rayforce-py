# Test

Rayforce-Py has a number of tests which are accessible with the makefile command:
```bash
make test
```

- This command runs a pytest suite against the:
    - Scalar types
    - Container types
    - Their descriptors
    - Queries
